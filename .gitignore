# dependencies / build
node_modules/
dist/
*.tsbuildinfo

# caches and local state
.cache/
scripts/.cache/
*.log
.DS_Store
.env
.env.*

# server bundles the built web UI + basemap into public/ at build time
packages/server/public/

# internal notes never ship in this public repo
notes/
NOTES-*.md
